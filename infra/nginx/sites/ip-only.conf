upstream backend_app  { server backend:8000; }
upstream frontend_app { server frontend:3000; }

# HTTP-only, IP-based access. For domain + SSL swap in maverickstech.conf.
server {
    listen 80 default_server;
    server_name _;

    limit_conn perip 50;

    # ---- static ----
    location /static/ {
        alias /var/www/static/;
        expires 1y;
        access_log off;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /var/www/media/;
        expires 30d;
        access_log off;
        add_header Cache-Control "public";
    }

    # ---- Django admin / API ----
    location /admin/ {
        limit_req zone=login burst=5 nodelay;
        proxy_pass http://backend_app;
        include /etc/nginx/proxy_params.conf;
    }

    location /api/v1/crm/public/leads/ {
        limit_req zone=public_lead burst=3 nodelay;
        proxy_pass http://backend_app;
        include /etc/nginx/proxy_params.conf;
    }

    location /api/v1/auth/login/ {
        limit_req zone=login burst=3 nodelay;
        proxy_pass http://backend_app;
        include /etc/nginx/proxy_params.conf;
    }

    location ~ ^/(api|ckeditor5|schema)/ {
        limit_req zone=api_anon burst=20 nodelay;
        proxy_pass http://backend_app;
        include /etc/nginx/proxy_params.conf;
    }

    # ---- Next.js frontend ----
    location /_next/static/ {
        proxy_pass http://frontend_app;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location / {
        proxy_pass http://frontend_app;
        include /etc/nginx/proxy_params.conf;
    }
}
